server:
  port: 8080
spring:
  application:
    name: assigment-service
    description: Spring boot service, which exposes a API to return pokemons.
    contact-name: Rajesh Iyer
    contact-email: iyerajesh@gmail.com
    version: 1.0

logging:
  level:
    root: INFO
management:
  endpoint:
    health:
      probes:
        enabled: true
